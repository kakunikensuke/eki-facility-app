# 住みやすさ駅前スコア（駅周辺施設検索アプリ） 要件定義書

サービス正式名称: **住みやすさ駅前スコア**（2026-07-16確定）。プロジェクトフォルダ名`eki-facility-app`は開発上の内部名称としてそのまま使用する。

作成日: 2026-07-16
ステータス: 確定（保留事項は全てユーザー確認済み、実装フェーズに移行）

---

## 1. 背景・目的

駅名を入力すると、その駅周辺にある **コンビニ・病院・スーパー・飲食店** の店舗数がカテゴリ別に表示されるアプリを開発する。

想定される利用シーン:
- 引っ越し・賃貸物件探しをしている人が、候補駅の生活利便性を数値でざっくり比較したい
- 出張・旅行前に、宿泊先の駅周辺に何があるか事前に把握したい
- 不動産業者が物件紹介の補足情報として使う

**最終目標**: 月1,000円以上の安定収益（広告＋アフィリエイト）を得ること。

このプロジェクトは既存の「ロッカーアプリプロジェクト（`ikebukuro-locker-app`）」とは別の新規アプリとして、`駅周辺施設検索アプリプロジェクト/eki-facility-app/` 配下で開発する。

---

## 2. ターゲットユーザー

| ユーザー像 | ニーズ |
|---|---|
| 引っ越し・部屋探し中の人 | 候補駅の生活利便性を素早く数値で比較したい |
| 不動産業者・仲介者 | 物件紹介時に周辺環境を客観的な数字で説明したい |
| 旅行者・出張者 | 宿泊駅周辺の利便性を事前に知りたい |

---

## 3. 収益化方針

ロッカーアプリで実践している「①アフィリエイト＋②広告表示」の2本柱を踏襲する。

1. **広告表示**（Google AdSense等）
   閲覧数（ページビュー）に比例して収益が発生する。このアプリは「駅名 × カテゴリ」の組み合わせ数だけ検索需要が存在しうるため、各駅ページをきちんとGoogle検索に拾ってもらう設計（SEO対策）が収益の生命線になる。
2. **アフィリエイト**
   引っ越し・不動産系サービス（例: 賃貸物件サイト、引っ越し業者比較サイト等）との相性が良い可能性がある。具体的な提携先は本アプリのコンテンツが一定量揃い、公開URLが取得できてから調査・申込を行う（ロッカーアプリの ecbo cloak 提携と同じ流れ）。
3. 月1,000円という目標額自体は大きくないため、**広告だけでも一定のアクセス数（目安として月数千PV程度）が確保できれば到達しうる規模**と考えられる。まずはSEOで拾われる駅ページ数を増やすことを優先する。

> 補足: 広告・アフィリエイトいずれも「審査が通るコンテンツ量・公開ドメインがあること」が前提条件になる。ロッカーアプリと同じ順序（①中身の充実→②正式公開→③審査申込）で進める想定。

---

## 4. プラットフォーム方針

**Web先行で開発する**（iOSアプリ化は保留）。

理由:
- このアプリの収益モデルは「駅名＋カテゴリで検索した人が検索結果からたどり着く」ことが軸になる。アプリストアからのインストールが必要なiOSより、検索エンジンから直接開けるWebの方が圧倒的に流入効率が良い。
- ロッカーアプリでも同じ理由（SEO流入→即コンバージョンの導線を重視）でWeb先行・iOS保留の方針を採用しており、その判断はそのままこのアプリにも当てはまる。
- iOSはストア審査・年間登録費・保守コストが継続的にかかる一方、月1,000円規模の収益目標に対しては投資対効果が見合わない。
- ホーム画面への追加やプッシュ通知が将来的に必要になった場合は、ネイティブiOS化ではなく **PWA化**（Webサイトをスマホのホーム画面にアプリのように追加できる仕組み）をまず検討する。

---

## 5. データソース・データ範囲

**OpenStreetMap（以下OSM）の Overpass API** を利用する。

- OSMは世界中のボランティアが地図データを整備している無料の地図データベース。「Overpass API」はそのデータに対して「この座標の周辺にあるコンビニを教えて」のような問い合わせができる仕組み。
- **無料・商用利用可**。ただし「ODbL」というライセンスに従う必要があり、**データの出典（OpenStreetMapのコントリビューターであること）をアプリ内に明記する義務がある**（フッター等にクレジット表示）。
- Google Places APIのような有料サービスと比べると、地域によっては店舗の網羅性・情報の新しさが劣る場合がある。ただし開発・運用コストがゼロで済むため、収益目標（月1,000円）に対してコスト面のリスクが最小になる。まずはOSMで開始し、将来的に収益が安定した段階でGoogle Places API等への切り替え・併用を再検討する。

### カテゴリとOSMデータの対応（初期案）

OSMでは地図上の施設に「タグ」という分類ラベルが付いている。今回数えたい4カテゴリは以下のタグに対応する想定（実装時に実データで検証が必要）。

| カテゴリ | 対応するOSMタグ（初期案） |
|---|---|
| コンビニ | `shop=convenience` |
| 病院 | `amenity=hospital` + `amenity=clinic`（クリニック含む、2026-07-16確定） |
| スーパー | `shop=supermarket` |
| 飲食店 | `amenity=restaurant` + `amenity=cafe` + `amenity=fast_food`（カフェ・ファストフード含む、2026-07-16確定） |
| ドラッグストア（2026-07-16追加、スコア対象外） | `shop=chemist` |
| 公園（2026-07-16追加、スコア対象外） | `leisure=park` |
| 保育園・幼稚園（2026-07-16追加、スコア対象外） | `amenity=kindergarten`（OSM上で保育園と幼稚園を区別するタグが存在しないため統合。池袋駅周辺の実データで確認済み） |

---

## 6. 集計範囲の定義

**「徒歩◯分圏内」を、駅の座標からの直線距離で概算する方式**を採用する。

- 厳密な道路網に沿った徒歩ルート計算（Google Maps等が使う経路探索）はコスト・実装難易度が高いため、まずは直線距離による概算で十分とする。
- 換算式: `半径(m) = 徒歩分数 × 80m/分`（一般的な成人の平均歩行速度を採用）
  - 例: 徒歩5分 → 半径400m／徒歩10分 → 半径800m
- **デフォルトの集計範囲は徒歩10分圏内（半径800m）に確定**（2026-07-16）。

---

## 7. 対象エリア

ロッカーアプリと同じく **主要駅から少数スタートし、段階的に拡大する方式**を採用する。

**初期対象駅は、ロッカーアプリ（ikebukuro-locker-app）と同じ7駅に確定**（2026-07-16）: 池袋・新宿・渋谷・東京・品川・上野・横浜。座標データもロッカーアプリの`stations.js`から流用する。同一駅にすることで、両アプリのデータ・実績を比較しやすくする狙いもある。
- OSMベースであれば技術的には全国の駅を対象にすることも可能だが、データ品質の検証・初期構築コストを抑えるため、まずは少数駅で仕組みを検証してから拡大する。
- **2026-07-17、ロッカーアプリの349駅（23都道府県）全件に拡大**。目的は対象エリアの充実そのものというより、住みやすさスコアのtarget値（8.1.1参照）を大都市駅だけでなく地方都市・郊外駅も含む実データ分布から決め直すこと。Overpass APIの504エラーにより8駅は初回未取得だが、翌日以降の定期バッチで自然に埋まる想定のため許容している。

---

## 8. 機能要件

### 8.1 MVP（第一弾でリリースする機能）

- **駅名検索**（プルダウン選択、将来的に入力補完も検討）
- **カテゴリ別店舗数の表示**（コンビニ／病院／スーパー／飲食店の4カテゴリを、駅ごとに数値で表示）
- **追加カテゴリの表示**（2026-07-16追加。ドラッグストア／公園／保育園・幼稚園を軒数のみ表示。住みやすさスコアには含めない「その他の施設」として区別する。詳細は設計書5.2参照）
- **住みやすさスコアの表示**（2026-07-16追加、詳細は8.1.1参照）
- **駅比較機能**（2026-07-17追加、詳細は8.1.2参照）
- **お気に入り駅機能**（2026-07-18追加、詳細は8.1.3参照）
- **法務ページ（プライバシーポリシー・利用規約）**（2026-07-18追加、詳細は8.1.4参照）
- **集計条件の明示**（「徒歩◯分圏内（直線距離換算）」であることを画面上に明記し、誤解を防ぐ）
- **データ出典表示**（OpenStreetMapのクレジット表示、ODbLライセンス対応）
- **広告枠プレースホルダー**（審査前提として、まずダミー枠のみ設置）

#### 8.1.1 住みやすさスコア（2026-07-16追加）

GUIデザイン刷新（参考: Lifesumアプリのダッシュボード画面）の一環として、4カテゴリの店舗数を集約した「住みやすさスコア」（0〜100点）を新規導入した。

- カテゴリごとに「これだけあれば生活に十分」という目安の軒数（target）を設定し、実際の軒数がtargetに対してどれだけ充足しているかを0〜25点で採点、4カテゴリ合計で0〜100点にする（算出ロジックの詳細は設計書参照）。
- 初期リリース時のtargetは統計的根拠のない**暫定値**（大都市駅7駅の実データを見た目安）だった。**2026-07-17、対象349駅（341駅取得成功）の実データに基づき、「各カテゴリの75パーセンタイル値（上位25%の駅が満点になる基準）」へ再設定した**（コンビニ27・スーパー8・病院7・飲食店140。詳細は設計書5.1参照）。
- 対象駅内での相対評価（最大値=100点とする正規化）ではなく、絶対的なtargetを採用した。理由: 相対評価だと駅を追加するたびに既存駅のスコアが変動してしまい、ユーザーへの一貫性を損なうため。
- 画面上には「スコアはOpenStreetMapのデータに基づく目安である」旨の免責表示を行う（実店舗数と同様の扱い）。

#### 8.1.2 駅比較機能（2026-07-17追加）

「引っ越し検討者が今の駅・候補駅同士を比較して判断する」という本アプリの中心的な想定利用シーンに対応するため、専用の比較ページを新設した。

- **比較駅数は2駅固定**。3駅以上の同時比較は行わない（UIのシンプルさを優先）。
- **「今住んでいる駅」の保存機能は持たない**。比較のたびにA・B2駅とも選び直す方式とし、ログインや駅の保存機能は不要と判断した。
- 専用ページ（`/compare`）でA・B駅の選択と比較結果（住みやすさスコア、カテゴリ別軒数）を1画面にまとめて表示する。ボトムナビ「比較」タブから遷移する。
- カテゴリ別軒数比較は、住みやすさスコア対象の4カテゴリと表示専用の3カテゴリ（8.1参照）の全7種を対象とする。

#### 8.1.3 お気に入り駅機能（2026-07-18追加）

ボトムナビ「お気に入り」タブに対応する機能として実装した。

- **保存先はブラウザのlocalStorageのみ**。ログイン・サーバー保存は行わない。駅比較機能（8.1.2）の「駅を保存しない」方針とは逆の「保存する」機能だが、アカウント基盤を持たない現状の設計方針に合わせ、サーバー同期は不要と判断した。
- 駅ページのヘッダー右上に☆ボタンを設置し、タップでお気に入り登録／解除（★⇄☆）できる。
- 専用ページ`/favorites`でお気に入り登録済みの駅一覧を表示し、各駅名から駅ページへ遷移できる。一覧側からも★ボタンで解除できる。
- 複数端末間の同期は行わない（同一ブラウザ内でのみ有効）。

#### 8.1.4 法務ページ（プライバシーポリシー・利用規約）（2026-07-18追加）

収益化準備（10章）の一環として、公開ドメイン取得前の段階から用意できる法務ページを先行実装した。

- 全ページ共通のフッターに`/privacy`へのリンクを設置。
- 運営者表記は個人開発である旨のみ記載し、実名・ハンドルネームは記載しない（ユーザー確認済み）。
- 問い合わせ手段はメールアドレスの公開ではなく、ページ内フォームの案内のみとした。フォーム自体は送信先が未実装のダミー（入力欄・送信ボタンとも`disabled`）で、「準備中」である旨を明記している。
- 記載内容: 運営者について／収集する情報（お気に入りのlocalStorage保存、アクセス解析未導入、将来の広告Cookie利用の可能性）／データの出典・正確性についての免責／サービス利用の免責事項／問い合わせ（フォーム準備中）。

### 8.2 将来拡張候補（優先度未定、MVP後に検討）

- ~~カテゴリの追加（ドラッグストア、公園、保育園）~~ → **2026-07-16対応済み**（8.1参照、保育園はOSMデータの制約上「保育園・幼稚園」に統合）。銀行は生活利便性への影響が小さいと判断し見送り。
- ~~駅同士の比較機能~~ → **2026-07-17対応済み**（8.1.2参照）
- **店舗一覧・地図表示（個々の店舗名や位置まで見られるようにする）→ 2026-07-18、実装を保留と判断**。理由: 本アプリの核心的な利用シーンは「引っ越し検討者が駅同士の店舗数・住みやすさスコアを比較して判断する」ことであり（8.1.2参照）、個々の店舗名・位置まで見せる地図機能はこの用途からは外れると判断した。加えて現在の集計バッチはOverpass APIから件数(count)のみ取得しており、個々の店舗の名称・座標までは保存していないため、実現するには集計方式自体の見直しが必要になる。ボトムナビにも「地図」タブは設置していない（従来あったダミータブは撤去済み）。
- ~~お気に入り駅機能~~ → **2026-07-18対応済み**（8.1.3参照）
- 集計範囲（徒歩分数）をユーザーが選択できるようにする
- 多言語対応（ロッカーアプリと同様、英語対応を優先候補とし、需要が見えてから判断）
- ~~住みやすさスコアのtarget値の見直し（実データ蓄積後）~~ → **2026-07-17対応済み**（8.1.1参照）

---

## 9. 非機能要件

- **SEO**: 駅ごとに固有のURLを持たせ、meta description・構造化データ（schema.org等）を設定する。sitemap.xml / robots.txt も整備する。
- **パフォーマンス・API負荷対策**: Overpass APIは無料の共同利用インフラのため、リクエスト頻度に配慮する必要がある（詳細は設計書参照）。ユーザーの検索のたびにリアルタイムでOverpass APIを叩くのではなく、事前に集計したデータをキャッシュして返す設計とする。
- **ライセンス遵守**: OSMデータの利用条件（ODbL）に従い、出典表示を必ず行う。
- **正確性に関する免責**: 店舗数はOSMデータに基づく目安であり、実際の店舗数と差異が生じうる旨を画面上に明記する。

---

## 10. 収益化に必要な準備（ロッカーアプリの知見を踏襲）

1. ~~コンテンツの充実（対象駅数・カテゴリの拡充）~~ → **対象349駅・カテゴリ7種まで拡充済み**（7章・8.1参照）
2. 正式リリース（公開ドメイン・ホスティングの用意）— 未着手。ドメイン取得・費用等はユーザー自身の意思決定が必要なため、着手する際は改めて相談する。
3. ~~プライバシーポリシー等の法務ページ整備~~ → **2026-07-18対応済み**（8.1.4参照）。ドメイン取得前でも用意できる範囲を先行実装した。
4. 広告（AdSense等）審査申込 — 公開ドメインが前提条件のため未着手。
5. アフィリエイト提携先の選定・審査申込 — 公開ドメインが前提条件のため未着手。

---

## 11. 開発方針との整合性

- ルートの`CLAUDE.md`にある「未定事項を実装で先回りして決めない」方針に従い、本要件定義書に記載した「保留・未決定事項」は実装前にユーザーと確定させる。
- 「Web版とiOS版でコア機能・仕様の整合性を保つ」という全体方針について、ロッカーアプリでは「当面WEBアプリのみでリリースしiOS化は保留」という形で一時適用除外としている。本アプリも同様の理由（SEO軸の収益モデルとの相性）でWeb先行とするため、同じ扱い（iOS版が存在しない間は本ルールの適用対象外）とする。

---

## 12. 確定事項（2026-07-16）・残る保留事項

初版で保留としていた項目は以下の通り確定した。

1. デフォルトの集計範囲 → **徒歩10分圏内（半径800m）**
2. 初期対象駅リスト → **ロッカーアプリと同じ7駅**（池袋・新宿・渋谷・東京・品川・上野・横浜）
3. 病院・飲食店カテゴリの範囲 → **クリニックを病院に含む／カフェ・ファストフードを飲食店に含む**
4. アプリの正式名称 → **「住みやすさ駅前スコア」**

残る保留事項（実装には必須でないため、公開準備フェーズで着手）:

- **アフィリエイト提携先の具体的な候補調査** — 引っ越し・不動産系サービス等、公開後に着手

---

## 関連ドキュメント

- 技術的な実現方式は [設計書.md](./設計書.md) を参照
