# お問い合わせをGoogleフォームからサイト内フォームに移行する（引き継ぎ）

作成日: 2026-08-15
依頼元: ユーザー（japan-proxy-cost で同じ移行を実施済み。その方式に揃える）

**実施状況: 2026-08-15 完了（`2129ed5` / `858fa4a`）。残りは §6 のGoogleフォーム削除のみ（ロッカーアプリの移行後）。**

---

## 0. 実施結果と、書かれていなかった問題（後から追記）

### `_next` は効かない

**§3-5 の `_next` による送信後リダイレクトは機能しなかった。** 本番（eki.kakuni-lab.com）から実送信して確認済み。送信自体は成功するが、FormSubmitの英語の完了ページ（"Thanks!"）が表示され、サイトを離れてしまう。

- ローカルからの送信でも同じ。**送信元ドメインと `_next` のドメインが一致していても効かない**
- FormSubmitの公式ドキュメントに制約の記載はなく、原因は特定できていない

対応として、`_next` に依存しない作りに変えた（`858fa4a`）。

- AJAXエンドポイント（`https://formsubmit.co/ajax/<hash>`）へ `fetch` で送り、成功したらreact-routerで `/contact-received` にサイト内遷移する
- `action` 属性と `_next` は残してある。プリレンダされた静的HTML（JSが動く前や、JSを実行しないクローラが見る状態）では素のPOSTとして機能するため、**JSが失敗しても送信手段そのものは失われない**
- `fetch` が失敗した場合も `form.submit()` で素のPOSTにフォールバックする

⚠ **japan-proxy-cost も同じ構成なので、同じ問題を抱えている可能性が高い。** 確認したところ、エンドポイント・`_next` の指定方法・`/contact-received` の存在まで同一だった。あちらでも送信後にFormSubmitの完了ページに飛んでいないか要確認。

### エイリアスは別ドメインからでも使える（§2の未検証事項）

`21c6f56659051072bab367d0af9fb0bc` は japanproxy.kakuni-lab.com で有効化されたものだが、**eki.kakuni-lab.com からの送信でも問題なく通った**。再有効化は不要で、実施もしていない。

### フォームの項目定義は1箇所に集約した（§3-5からの変更）

引き継ぎ書はReact側とプリレンダ側の両方に同じHTMLを書く指示だったが、それだと必ずズレる。項目定義を `frontend/src/content/pages.js` の `CONTACT_FORM_FIELDS` / `CONTACT_FORM_HIDDEN` に置き、両者がそこから `<form>` を組む形にした。

---

## 1. 何をするのか

いまの `/about` と `/privacy` は、お問い合わせを**外部のGoogleフォームへのリンク**で済ませている。
これを **サイト内に置いた実物の `<form>`** に差し替える。送信の中継には
[FormSubmit](https://formsubmit.co/) を使う（バックエンドを持たない方針を維持するため）。

あわせて、**作成済みのGoogleフォーム本体も削除する**（ユーザーの依頼。手順は §6）。

### なぜ変えるのか

- 外部リンクだと、押した時点でサイトを離れる。連絡手段としては動くが、審査の観点では
  「サイト内に連絡手段が無い」と見られる余地が残る
- japan-proxy-cost が 2026-08-14 に同方式へ移行済み。**3アプリで送信先を1つに揃えると管理が楽**
  （届いたメールの件名で発信元アプリを見分ける）

---

## 2. 送信先（すでに有効化済み。新たな登録は不要）

```
https://formsubmit.co/21c6f56659051072bab367d0af9fb0bc
```

- この文字列は kakuniadao7@gmail.com のエイリアス（ハッシュ）。**メールアドレスそのものはHTMLに出ない**
- japan-proxy-cost で 2026-08-14 に有効化済み。**再有効化の操作をしてはいけない**
  （FormSubmitは新しい有効化リクエストを出すと前のトークンが無効になる。
  japan-proxy-cost のフォームが止まる）

### ⚠ 着手して最初にやること: 疎通確認

同じエイリアスを**別ドメインから**使えるかは未検証。FormSubmitは既定ではドメイン制限を
かけないはずだが、確認せずに本番へ出さないこと。

1. ローカルビルドの `/about` からテスト送信を1通
2. kakuniadao7@gmail.com に届くか確認
3. 届かない・エラーになる場合は、**そこで止めてユーザーに報告する**。
   勝手に有効化リクエストを送ると japan-proxy-cost 側が巻き添えで止まる

---

## 3. 実装（このリポジトリの構造に合わせた手順）

### 3-1. 大前提: Reactとプリレンダの両方に足すこと

`frontend/src/content/pages.js` の先頭コメントにある通り、本文は
**React側（`components/ContentBlocks.jsx`）とプリレンダ側（`scripts/prerender.js` の `blocksToHtml`）
の両方が同じブロック配列を描く**構造になっている。

**片方だけに足すと、静的HTMLと画面の中身がズレる。** `createRoot` は `#root` の中身を
丸ごと捨てて描き直すので、プリレンダ側にだけフォームを足しても、ブラウザで開いた瞬間に消える。

→ 新しいブロック型 `contactForm` を作り、**2ファイル同時に**対応させる。

### 3-2. `frontend/src/content/pages.js`

`CONTACT_FORM_URL`（Googleフォーム）を削除し、FormSubmitの送信先に置き換える。

```js
// お問い合わせフォームの送信先（FormSubmit）。メールアドレスのエイリアスで、
// 生のアドレスはHTMLに出ない。japan-proxy-cost・ロッカーアプリと同じ送信先を共用し、
// 件名（_subject）で発信元アプリを見分ける。
// ここを空文字にするとお問い合わせ欄ごと出さない（動かないフォームは置かない方針）。
export const CONTACT_FORM_ENDPOINT =
  "https://formsubmit.co/21c6f56659051072bab367d0af9fb0bc";

export function contactBlocks() {
  if (!CONTACT_FORM_ENDPOINT) return [];
  return [
    { type: "h2", text: "お問い合わせ" },
    {
      type: "p",
      text: "ご意見・ご要望、データの誤りのご指摘、掲載内容に関するお問い合わせは、以下のフォームからお願いします。個人で運営しているため、返信までに数日いただくことがあります。",
    },
    { type: "contactForm" },
  ];
}
```

### 3-3. `frontend/src/components/ContentBlocks.jsx`

`case "contactForm":` を追加。中身は素の `<form>` でよい（Reactの状態管理は不要。
FormSubmitへ直接POSTする）。

### 3-4. `frontend/scripts/prerender.js`

`blocksToHtml` に同じ `case "contactForm":` を追加し、**文字列として同一のHTML**を出す。

### 3-5. フォームの中身（両方で揃える）

```html
<form class="contact-form" action="{CONTACT_FORM_ENDPOINT}" method="POST">
  <input type="hidden" name="_subject" value="住みやすさ駅前スコア — お問い合わせ" />
  <input type="hidden" name="_captcha" value="false" />
  <input type="hidden" name="_template" value="table" />
  <input type="hidden" name="_next" value="https://eki.kakuni-lab.com/contact-received" />
  <!-- ボット除け。人間には見えない欄で、埋まっていたら送信を捨てる -->
  <input type="text" name="_honey" style="display:none" tabindex="-1" autocomplete="off" />

  <label>
    <span>お問い合わせの種類</span>
    <select name="種類" required>
      <option value="データの誤りの指摘">データの誤りの指摘</option>
      <option value="駅の追加リクエスト">駅の追加リクエスト</option>
      <option value="掲載内容について">掲載内容について</option>
      <option value="その他">その他</option>
    </select>
  </label>

  <label>
    <span>該当ページ <em>（特定の駅の話なら、そのページのURL）</em></span>
    <input type="url" name="該当ページ" placeholder="https://eki.kakuni-lab.com/..." />
  </label>

  <label>
    <span>内容</span>
    <textarea name="内容" rows="6" required placeholder="何がどう違っていたか、正しくはどうあるべきかを書いてください"></textarea>
  </label>

  <label>
    <span>参照元 <em>（任意。公式ページのURL等があると修正が早いです）</em></span>
    <input type="url" name="参照元" placeholder="https://..." />
  </label>

  <label>
    <span>メールアドレス <em>（任意。返信が必要な場合のみ）</em></span>
    <input type="email" name="email" placeholder="you@example.com" />
  </label>

  <button type="submit">送信する</button>
</form>
<p>送信内容は FormSubmit（外部の中継サービス）経由で届きます。このサイトには保存されません。</p>
```

**`_subject` はアプリごとに必ず変えること。** 3アプリの問い合わせが同じ受信箱に届くので、
件名だけが発信元を見分ける手がかりになる。

**`name` 属性は日本語のままでよい。** FormSubmitはそのまま項目名としてメールに出す。

### 3-6. 送信後ページ `/contact-received` を作る

`_next` の飛び先。**存在しないと送信後に404**になるので必ず作る。

- `scripts/prerender.js` の `STATIC_PAGES` に追加し、静的HTMLを出す
- React側にもルートを足す（直接開かれても表示できるように）
- **中身は薄くしないこと。** 「送信されました」の1行だけだと、
  2026-08-08にAdSenseで指摘された「有用性の低いコンテンツ」を自分から増やすことになる。
  受け取った後どうするか（誤りの指摘は公式ソースを見てから直す、返信の目安、
  アドレス未記入なら返信は無いが内容は読む 等）を書いて、250語相当以上にする
- 固定ページ相互リンク（`staticPageFooterLinks`）にも入れて行き止まりにしない

### 3-7. `frontend/src/index.css`（または該当CSS）

`.contact-form` のスタイルを追加。japan-proxy-cost の `src/styles.css` の
`/* お問い合わせフォーム */` ブロックをそのまま流用してよい。

---

## 4. 確認手順

1. `npm run build` が通る
2. **`dist/about.html` を直接 grep して `<form` が入っていること**（プリレンダ側の抜けを検出）
3. ブラウザで `/about` を開き、**Reactが載った後もフォームが消えていないこと**（ここが本命）
4. `dist` 内の全HTMLに `docs.google.com/forms` が1件も残っていないこと
5. `/contact-received` が200で開くこと
6. 実際に1通送信し、Gmailに届くこと。件名が「住みやすさ駅前スコア — お問い合わせ」であること
7. **HTMLに `kakuniadao7@gmail.com` が出ていないこと**（エイリアスを使う目的そのもの）

---

## 5. やってはいけないこと

- **FormSubmitの有効化リクエストを送らない**（§2参照。japan-proxy-cost が止まる）
- **ファイルを直接削除しない。** 不要になったファイルは `削除用フォルダ/` に移動する
  （リポジトリ直下のCLAUDE.mdのルール）
- **`_subject` を他アプリと同じにしない**

---

## 6. Googleフォーム本体の削除（ユーザーの作業）

コードからURLを消しても、Googleフォーム自体は残る。ユーザーに以下を依頼すること。

**ただし、削除は §4 の確認が全て通ってからにする。** 先に消すと、
新フォームが動かなかったときに連絡手段がゼロになる。

対象フォームのID: `1FAIpQLSetahrVFYNfl0WBCeZhbpyJV4wj8hXt7Uoq-JsGzfNfQCFC9A`

1. https://docs.google.com/forms/ を開く
2. 「住みやすさ駅前スコア」のお問い合わせフォームを探す
3. 既に回答が入っていないか確認する（入っていれば先に内容を読む）
4. 右クリック →「ゴミ箱に移動」

⚠ **このフォームURLはロッカーアプリ（ikebukuro-locker-app）でも共用している。**
ロッカー側の移行が終わる前に消すと、ロッカーアプリの問い合わせリンクが死ぬ。
**両アプリの移行が完了してから削除すること。**

---

## 7. 関連

- 実装の見本: `海外向けアフィリエイトプロジェクト/japan-proxy-cost/src/lib/pages.mjs` の
  `/contact` ページ（`CONTACT_FORM_ENDPOINT` で検索）
- 同じ移行のロッカーアプリ版: `ロッカーアプリプロジェクト/ikebukuro-locker-app/お問い合わせフォーム移行_引き継ぎ.md`
- 本番URL: https://eki.kakuni-lab.com
