# 住みやすさ駅前スコア（駅周辺施設検索アプリ） 設計書

作成日: 2026-07-16
前提: [要件定義書.md](./要件定義書.md) の内容に基づく技術設計。集計範囲（徒歩10分/800m）・初期対象7駅・カテゴリ範囲・正式名称は全て確定済み（要件定義書12章参照）。

---

## 1. 全体構成

```
[ブラウザ]
   │  駅名で検索
   ▼
[フロントエンド (React + Vite)]
   │  駅スラッグ(slug)を指定してAPIを呼ぶ
   ▼
[バックエンド (Node.js + Express)]
   │  事前集計済みのJSONファイルを返すだけ（原則リアルタイム外部通信なし）
   ▼
[集計済みデータ (facility-counts.json)]
   ▲
   │  1日1回、定期バッチが更新
[集計バッチ (Node.jsスクリプト)]
   │  駅ごとにOverpass APIへ問い合わせ
   ▼
[Overpass API（OpenStreetMapの外部サービス）]
```

技術スタックはロッカーアプリ（React/Vite + Node.js/Express）と揃えることで、これまでの実装・運用ノウハウ（デプロイ方法、i18n、SEO対応パターン等）をそのまま再利用できるようにする。

---

## 2. なぜ「検索のたびに外部APIを呼ぶ」設計にしないか

一見、ユーザーが駅名を入力するたびにOverpass APIへ直接問い合わせれば単純に見えるが、以下の理由から**事前バッチ集計＋キャッシュ方式**を採用する。

- Overpass APIは無料の共同利用インフラであり、利用者全体で共有されるレート制限（一定時間あたりのリクエスト数上限）がある。アクセスが増えるたびに毎回問い合わせると、制限に達してアプリ全体が使えなくなるリスクがある。
- 同じ駅への検索が何度来ても、店舗数の実態はそう頻繁には変わらない（コンビニが1日に何軒も開店・閉店するわけではない）ため、リアルタイム性より安定性を優先する。
- ロッカーアプリの「駅単位で定期的に自動更新するバッチ（`updateLockers.js`）」と同じ考え方であり、実装パターンを流用できる。

→ **ユーザーからのリクエストは常に「集計済みデータを読むだけ」**にし、Overpass APIへの問い合わせはバッチ処理に閉じ込める。

---

## 3. 集計バッチの設計

### 3.1 処理の流れ（駅1件あたり）

1. 対象駅マスタ（`stations.json`）から駅の緯度経度を取得
2. 徒歩分数→半径(m)に換算（`半径 = 徒歩分数 × 80m`、要件定義書6章参照）
3. Overpass APIに「その座標を中心に半径◯m以内にある、指定タグの施設」を問い合わせるクエリを送信
4. カテゴリ（コンビニ／病院／スーパー／飲食店／ドラッグストア／公園／保育園・幼稚園）ごとに件数を集計
5. 結果を`facility-counts.json`に保存（駅slugをキーにしたオブジェクト）

### 3.2 Overpass APIクエリのイメージ

Overpass APIには「Overpass QL」という専用のクエリ言語で問い合わせる。イメージは以下の通り（実装時に微調整する）。

```
[out:json][timeout:25];
(
  node["shop"="convenience"](around:800, 35.7295, 139.7109);
  node["shop"="supermarket"](around:800, 35.7295, 139.7109);
  node["amenity"="hospital"](around:800, 35.7295, 139.7109);
  node["amenity"="restaurant"](around:800, 35.7295, 139.7109);
);
out count;
```

- `around:800, 緯度, 経度` の部分が「この座標から半径800m以内」を意味する
- `out count;` で該当件数のみを取得できる（個々の店舗詳細は不要なため、通信量を抑えられる）

### 3.3 バッチの実行頻度・マナー

- 実行頻度は1日1回を初期案とする（対象駅数が少ないうちは頻度を上げても問題ない）
- Overpass APIの利用ルール（fair use policy）に従い、リクエスト間隔を空ける（例: 駅ごとに数百ms〜1秒程度のインターバル）・User-Agentを明示する等、ロッカーアプリの`updateLockers.js`で実践したマナーを踏襲する
- 取得に失敗した駅は既存データを保持し、他の駅の処理は継続する（ロッカーアプリと同じ「部分失敗を許容する」設計）

---

## 4. データ設計

### 4.1 駅マスタ `backend/data/stations.json`

```json
[
  { "slug": "ikebukuro", "name_ja": "池袋", "lat": 35.7295, "lon": 139.7109 }
]
```

ロッカーアプリの`stations.js`と同様の構成。将来英語対応する場合は`name_en`を追加する想定（要件定義書の対象外・将来拡張候補）。

### 4.2 集計結果 `backend/data/facility-counts.json`

```json
{
  "ikebukuro": {
    "walk_minutes": 10,
    "radius_m": 800,
    "counts": {
      "convenience_store": 23,
      "hospital": 5,
      "supermarket": 4,
      "restaurant": 112,
      "drugstore": 6,
      "park": 9,
      "nursery": 3
    },
    "updated_at": "2026-07-16T09:00:00+09:00",
    "source": "OpenStreetMap contributors (ODbL)"
  }
}
```

- `walk_minutes` / `radius_m` を結果に含めることで、フロント側で「どの範囲を集計したか」を必ず併記できるようにする（要件定義書8.1「集計条件の明示」に対応）
- `source`をレコードごとに保持し、フロント表示・法務対応の両面で出典を追跡できるようにする

---

## 5. バックエンドAPI設計

| メソッド/パス | 説明 |
|---|---|
| `GET /api/stations` | 対象駅一覧（slug・駅名）を返す |
| `GET /api/facility-counts?station=<slug>` | 指定駅の集計済みカテゴリ別店舗数＋住みやすさスコアを返す（`facility-counts.json`を読み、スコアはリクエスト時に算出） |

- ロッカーアプリの`/api/stations`・`/api/lockers`と対になる構成。API設計の型を揃えることで実装・保守の負担を下げる。

### 5.1 住みやすさスコアの算出（2026-07-16追加、2026-07-17target値更新）

`backend/scoring.js`にロジックを集約。カテゴリごとに以下のtarget（この軒数あれば満点）を設定し、`min(実軒数/target, 1) × 25点`を4カテゴリ分合計してスコア（0〜100点、小数点以下四捨五入）とする。

| カテゴリ | target（満点となる軒数） |
|---|---|
| コンビニ | 27軒 |
| スーパー | 8軒 |
| 病院（クリニック含む） | 7軒 |
| 飲食店（カフェ・ファストフード含む） | 140軒 |

- 初期リリース時のtarget（コンビニ30・スーパー10・病院15・飲食店400）は、大都市駅のみ7駅の実データを見た**暫定値**（統計的根拠なし）だった。2026-07-17、対象駅を349駅（341駅でデータ取得成功）に拡大した実データを基に、**「各カテゴリの75パーセンタイル値（上位25%の駅で満点になる基準）」に統一**して再設定した。大都市駅だけで決めるとコンビニ・飲食店等の目安が高く出過ぎる（地方・郊外駅がほぼ満点に届かない）という課題があったため。
- 事前バッチ（`updateFacilityCounts.js`）ではなく、APIリクエスト時（`server.js`）に算出する設計にした。理由: targetの調整はバッチ再実行なしに即座に反映できた方が運用しやすく、計算コストも軽微（4カテゴリの単純な四則演算）なため。
- レスポンス形式: `score: { total: 88, breakdown: { convenience_store: 25, supermarket: 25, hospital: 13.3, restaurant: 25 } }`。`breakdown`は将来的にカテゴリ別の内訳バー表示（フロント側で採用済み、6章参照）に使う。

### 5.2 追加カテゴリ（2026-07-16追加、スコア対象外）

引っ越し検討者が駅を比較判断する材料を増やす目的で、ドラッグストア・公園・保育園・幼稚園の3カテゴリを追加した（`frontend/src/categories.js`の`EXTRA_CATEGORIES`）。

- `backend/scoring.js`の`SCORE_TARGETS`には含めない。ユーザー判断（2026-07-16）: 「スコアに含めると、既存4カテゴリと同じく根拠の薄いtarget値をさらに増やすことになるため、表示のみに留める」。
- `backend/batch/updateFacilityCounts.js`の`CATEGORY_TAGS`に追加しただけで、`counts`オブジェクトに新規キー（`drugstore`/`park`/`nursery`）が増える。`server.js`の`calculateScore()`は`SCORE_TARGETS`のキーのみを走査するため、コード変更なしにスコア計算からは自動的に除外される。
- フロント側は`breakdown-card`（スコア対象4カテゴリ、内訳バー付き）とは別に`extra-card`（「その他の施設」、バーなしの単純な軒数リスト）を新設して区別する。バーを設けなかったのは、上記のスコア除外理由と同じく「新たな目安値（何軒で満点扱いの基準）を増やさない」ため。
- カラーはdataviz skillの検証済みカテゴリカルパレット固定順5〜7（aqua/orange/violet）を使用（`--series-drugstore`等、`frontend/src/index.css`）。
- **保育園・幼稚園の統合について**: 実装時にOverpass APIで実データを確認したところ、OSM上には保育園と幼稚園を区別するタグが存在せず、いずれも`amenity=kindergarten`で登録されていた（池袋駅周辺で「南池袋保育園」「草苑幼稚園」が同一タグと確認）。`amenity=childcare`という別タグも試したが該当0件だった。ユーザー確認の上、カテゴリ名を「保育園・幼稚園」に変更して統合表示する方針とした（クリニックを病院に含める、カフェ・ファストフードを飲食店に含めるのと同じ扱い）。

---

## 6. フロントエンド設計

- **技術**: React + Vite（ロッカーアプリと共通）
- **画面構成（MVP）**:
  1. 駅選択画面（プルダウン、将来は入力補完）
  2. 結果画面: カテゴリ別カウント・住みやすさスコアを表示、集計範囲（徒歩◯分圏内）とデータ出典・更新日時を明記
- **ルーティング**: `/:stationSlug` で駅ごとに固有URLを割り当てる（SEO対策、ロッカーアプリの設計を踏襲）。`/compare`は駅比較ページ専用の固定パス（6.2参照）。

### 6.1 GUIデザイン（2026-07-16刷新、Lifesum参考）

初期実装のシンプルなカード表示から、健康管理アプリ「Lifesum」のダッシュボード画面（ユーザー提供のスクリーンショットを参照）を参考にしたレイアウトへ刷新した。

- **ヘッダー**: 緑・紫・オレンジのメッシュ状グラデーション背景（`radial-gradient`を3枚重ね）。中央に円形のスコアリング（住みやすさスコアを表示）、左右に「合計軒数」「集計範囲（徒歩◯分）」の補助数値を配置。Lifesumの「EATEN / カロリー数 / BURNED」の3点配置をスコア表示に転用したもの。
- **駅選択**: Lifesumの日付送りナビ（前後の日付に移動するUI）は採用せず、既存のプルダウン方式を維持（ユーザー判断、2026-07-16）。ヘッダー直下に配置。
- **内訳バー**: Lifesumの「Carbs/Protein/Fat」の3列バー表示を、4カテゴリの横棒グラフに転用。バーの色はdataviz skillの検証済みカテゴリカルパレット（固定順1〜4: blue/green/magenta/yellow）を使用し、CVD（色覚多様性）安全性を`scripts/validate_palette.js`で確認済み。カテゴリ名・軒数は文字表示も併記し、色だけに依存しない（コントラストWARNが出たmagenta/yellowに対するrelief rule対応）。
- **出典カード**: Lifesumの「Day rating」カード（絵文字＋一言コメント＋SEE MOREリンク）は、独自ロジックを増やさない方針でデータ出典・更新日時・免責事項の案内カードに置き換え。
- **ボトムナビ**: Lifesumの5タブ構成（Diary/Progress/+/Diets/Recipes）を参考に、「ホーム/比較/お気に入り」の3タブを実装（`frontend/src/components/BottomNav.jsx`）。地図タブは2026-07-18に実装を保留と判断し、設置していない（要件定義書8.2参照）。
- **カラートークン**: `frontend/src/index.css`の`:root`にライト/ダーク両対応のCSS変数（`--series-convenience`等）を定義し、ダークモード（`prefers-color-scheme: dark`）でも同じ検証済みパレットの別ステップを使用。
- **将来の多言語対応**に備え、UI文言はi18nライブラリ経由で管理する構成をMVP時点から採用しておく（ロッカーアプリの`locales/ja.json`方式を踏襲、当面は日本語のみ）

### 6.2 駅比較ページ（2026-07-17追加）

要件定義書8.1.2の駅比較機能を`/compare`ページとして実装（`frontend/src/pages/ComparePage.jsx`）。ユーザーとAskUserQuestionで合意した3つの設計判断:

- **比較駅数は2駅固定**。UIの複雑化を避けるため3駅以上には対応しない。
- **駅の選択は保存せず毎回選び直す**。ログイン・localStorage等の永続化は行わない。
- **専用ページに選択UIと結果を一画面でまとめる**（ステップ形式のウィザードや、駅詳細ページからの追加操作は不採用）。

実装上のポイント:

- バックエンドAPIの追加は行わず、既存の`GET /api/facility-counts?station=`を駅A・B分2回呼び出すことで実現。新規エンドポイントは作っていない。
- 表示内容は「住みやすさスコアの比較（スコアが高い方をハイライト）」と「カテゴリ別軒数比較（8.1の4カテゴリ＋8.1の追加3カテゴリ、計7種）」の2ブロック。同一駅を選んだ場合はガードメッセージを表示し比較結果は出さない。
- ボトムナビの「比較」タブから遷移する。「ホーム」タブは`/`固定ではなく、既に駅ページにいる場合は遷移しない（比較ページからのみ直前の駅ページへ戻る動作）。
- 初回リリース版のUIには「駅A」「駅B」ラベルと「VS」の区切り文字を表示していたが、ユーザーレビューで視認性・冗長性の指摘を受け削除。プルダウン2つを並べるのみのシンプルな構成に変更した（`aria-label`でアクセシビリティは維持）。あわせてヘッダータイトルの文字サイズ・太さ・影を強化し視認性を改善した。
- ヘッダー見出しのスタイル（`.compare-title`等）は、後述のお気に入りページと共用するため`.page-title`・`.subpage-header`という汎用クラス名に統一した（2026-07-18）。

### 6.3 お気に入り機能（2026-07-18追加）

要件定義書8.1.3のお気に入り駅機能を実装した。

- **保存先**: `frontend/src/favorites.js`でlocalStorage（キー`eki-facility-favorites`）に駅slugの配列を保存する。バックエンドAPIの追加は行っていない。
- 同一タブ内の複数コンポーネント（駅ページの★ボタン・`/favorites`一覧ページ）を同期させるため、変更時に`window`へカスタムイベント（`eki-facility-favorites-changed`）を発火し、`useFavorites()`フックがそれを購読して再描画する方式にした。Context等の状態管理ライブラリは導入せず、既存構成に合わせて最小限の実装とした。
- `frontend/src/pages/FavoritesPage.jsx`を新設し、`/favorites`ルートに割り当てた。お気に入りが0件の場合は登録方法を案内するメッセージのみを表示する。
- `StationPage.jsx`のヘッダー右上（`.favorite-toggle-header`）に☆/★ボタンを設置し、タップで現在の駅のお気に入り状態を切り替える。

---

## 7. SEO設計

- 駅ごとのページに`title`・`meta description`・OGPタグを動的設定（例: 「池袋駅周辺のコンビニ・病院・スーパー・飲食店の数」）
- `schema.org`構造化データの付与を検討（施設カテゴリごとの数値情報が検索結果でリッチに表示される可能性があるか要調査）
- `sitemap.xml`・`robots.txt`をビルド時に自動生成（ロッカーアプリの`generate-sitemap.js`と同じ仕組みを流用）

---

## 8. 収益化の実装設計

- **広告枠**: ロッカーアプリの`AdSlot.jsx`と同様に、まずダミーのプレースホルダーコンポーネントを結果画面下部に設置。AdSense審査が通り次第、実タグに差し替える。
- **アフィリエイト導線**: 現時点では提携先未定のため実装しない。要件定義書10章の順序（コンテンツ充実→公開→審査申込）に従い、提携が決まった段階でロッカーアプリと同じ「準備中バッジ付きボタン→提携後に実URLへ差し替え」のパターンを踏襲する。

---

## 9. ライセンス・法務対応

- OpenStreetMapのデータ利用条件（ODbL）に基づき、**全ページのフッター等に「地図データ: © OpenStreetMap contributors」の出典表示を必須**とする
- プライバシーポリシー等の法務ページは、広告審査申込のタイミングまでに整備する（要件定義書10章）

---

## 10. ディレクトリ構成案

```
駅周辺施設検索アプリプロジェクト/
└── eki-facility-app/
    ├── 要件定義書.md
    ├── 設計書.md
    ├── README.md            # 実装着手時に作成
    ├── CLAUDE.md            # 技術選定が正式に確定したタイミングで作成
    ├── backend/
    │   ├── server.js
    │   ├── scoring.js                    # 住みやすさスコア算出ロジック
    │   ├── batch/
    │   │   └── updateFacilityCounts.js   # Overpass API集計バッチ
    │   └── data/
    │       ├── stations.json
    │       └── facility-counts.json
    └── frontend/
        └── src/
            ├── categories.js             # カテゴリ表示情報(アイコン・ラベル)
            ├── favorites.js               # お気に入り駅（localStorage管理）
            ├── pages/                    # StationPage.jsx, ComparePage.jsx, FavoritesPage.jsx, NotFound.jsx
            └── components/               # AdSlot.jsx, BottomNav.jsx
```

---

## 11. 開発フェーズ・ロードマップ案

1. **フェーズ1（MVP構築）**: 対象駅を数駅に絞り、Overpass API集計バッチ・駅名検索・カテゴリ別カウント表示を実装
2. **フェーズ2（SEO対応）**: 駅ごとのURL・meta・sitemap整備
3. **フェーズ3（収益化準備）**: 広告枠プレースホルダー設置、法務ページ整備
4. **フェーズ4（対象駅拡大）**: 段階的に対象駅を増やす
5. **フェーズ5（本番リリース）**: 公開ドメイン取得・デプロイ、広告審査申込、アフィリエイト提携交渉

この順序は暫定案であり、着手前にユーザーと最終確認する（要件定義書「12. 保留・未決定事項」参照）。

---

## 12. リスク・注意点

- **Overpass APIの利用規約遵守**: 過度なリクエスト頻度を避け、fair use policyに従う。無料インフラであるため、将来的にアクセス数が増えた場合は自前のOSMデータミラー（大規模になれば検討）や有料APIへの切り替えも視野に入れる。
- **データ精度の限界**: OSMはボランティア整備のデータであり、地域によって店舗の網羅性にばらつきがある。「店舗数は目安である」旨を画面上に明記し、過度な精度保証をしない。
- **カテゴリ定義**: 「病院」はクリニック含む、「飲食店」はカフェ・ファストフード含む、で確定済み（要件定義書5章）。実装時はOSMの実データでこの分類が意図通りに機能するか検証する。
